# Details and concepts used

We're using Fluent pattern to build the core helper class named request-handle.ts
idea is each method will return the class itself using 'this' and hence all methods
can be used with method chain by .(DOT) notation
